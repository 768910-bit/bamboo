body {
  background: url('/img/bamboo.jpg') no-repeat center center fixed;
  background-size: cover;
  min-height: 100vh;
}

#page-header, #content-inner, #footer {
  background: rgba(255, 255, 255, 0.85);
  backdrop-filter: blur(10px);
  border-radius: 15px;
  margin: 20px;
  padding: 25px;
}
